<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta
			name="viewport"
			content="width=device-width, initial-scale=1.0"
		/>
		<title>Names</title>
	</head>
	<body>
		<h1><%= category_name %> Names</h1>
		<% if(names.length === 0){ %>
			<p>No Names Added Yet...</p>
		<% } else { %>
			<div>
			<% names.forEach(name => { %> <%- include('partials/name', {name: name})
			%> <%}) %>
		</div>
		<% } %>
		
	</body>
</html>
